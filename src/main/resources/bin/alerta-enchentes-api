#!/bin/bash

COMMAND=$1

case $COMMAND in
	start)
		nohup java -jar  target/alertas-enchentes-api-1.0.jar &
	;;
	stop)
		PID=$(pgrep alertas-enchentes-api)
		if [ -n "${PID}" ]; then
			kill $PID
		fi 
	;;
	*)
		echo "Usage: ./alerta-enchentes-api <start|stop>"
	;;
esac